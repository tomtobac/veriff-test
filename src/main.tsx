import React from "react";
import ReactDOM from "react-dom/client";
import CheckCompilance from "./components/CheckCompilance";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <CheckCompilance />
  </React.StrictMode>
);
